"""Tree-search algorithms for CRP-U.

This category includes both exact searches (B&B, IDA*, IDBB) and explicitly
bounded heuristic tree searches. Exactness is declared by each algorithm's
metadata rather than inferred from this directory.
"""
