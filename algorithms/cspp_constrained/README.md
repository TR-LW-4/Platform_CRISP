# cspp_constrained

Currently only the universal algorithms from `_shared/` are symlinked
here.  Problem-specific baselines can be added later:

* `heuristic/greedy/`       — depth-1 greedy (symlink)
* `evolutionary/genetic/`   — GA (symlink)
* `rl/ppo/`                 — PPO (symlink)
* `rl/reinforce/`           — REINFORCE (symlink)

See `algorithms/README.md` for the overall layout convention.
